


Edit the Routelink file to only assimilate the upstream gage.

```
library(rwrfhydro)
library(data.table)

domDir <- '/glade/scratch/jamesmcc/04233300/DOMAIN'
setwd(domDir)

rlOrig <- as.data.table(GetNcdfFile('RouteLink.nc', q=TRUE))

rlOrig[ trimws(gages) != '', ]


```
